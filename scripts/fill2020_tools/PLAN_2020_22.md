# 2020–2022 remaining rev cells — measured route plan (prepared 2026-08-10)

Scope: every open revS/revC member-quarter cell Mar-2020→Dec-2022 per point-in-time N500
(rebuilt from live data 2026-08-10 after the Mar-23→Dec-24 batch + MGL/PATANJALI heals landed).
**60 companies, 138 cells.** Evidence pre-staged: `scripts/_nselist/` caches now carry the FULL
exchange record (classic index + integrated era) for all 56 non-insurer names; the XBRL route was
dry-run over every cell (2026-08-10) and its per-cell refusals sit in `_nse_xbrl_rev_skips.json`.
The XBRL rung is DRY for this whole set (0 fillable) — these are the residue precisely because the
Aug-6 sweep drained that route. Routes below are measured, not guessed.

## A. Bank consolidated statements from results PDFs (§58, INDUSINDBK ×4 precedent) — ~15 cells
CANBK 2106,2109,2112 · RBLBANK con 2106,2109 + std 2112,2206 · INDIANB std 2009,2103 + con 2106 ·
MAHABANK 2106 · CENTRALBK 2009 · FEDERALBNK 2206,2209 · ICICIBANK 2209 · AXISBANK std 2209,2212.
Their BANKING_*.xml links 404 (never published) or are placeholders; the con statement pages in
the outcome PDFs read fine (IndusInd's did, scan+OCR text layer). Interest Earned convention;
anchor every column on stored XBRL values; elimination parity (con==std) is REAL for BC-model
banks — the document decides.

## B. Ordinary §58 PDF single/double cells (ANGELONE Mar-25 precedent) — ~35 cells
The Jun-2022-hole survivors and scattered singles: ABREL, ADANIENSOL, ADANIENT, EDELWEISS,
GMRAIRPORT, IOC, JINDALSTEL, JMFINANCIL, MMTC, RAIN, PEL(2112,2206), WELCORP(2112,2206),
IDEA(2206,2209), GSPL(2206,2212), CCAVENUE(2112,2206), HATSUN(2112,2203 — consolidates, E5),
ACC(2112), STAR(2112), TATACHEM(2112), EXIDEIND(2103), KSB(2109), ATUL std 2212, BSE std 2206,
PFC std 2206, SUNDARMFIN std 2206, ANGELONE std 2206, ABSLAMC std 2206, 360ONE std 2209,
JSL std 2209, NAM-INDIA std 2209, M&MFIN 2209, LICI 2209 con + 2212 std, ALOKINDS 2009,
BORORENEW 2203. Announcement PDFs exist (rows listed, XBRL placeholder). §55e warning: select the
attachment by CONTENT, not date order.

## C. Multi-quarter PDF campaigns — 12 cells
ZFCVINDIA ×8 (2003→2112): consolidates (E5: divergence back to 2018) — con statement pages of its
own quarterly PDFs; comparative columns close 2-3 cells per document.
TATASTLLP ×4 (2109→2206): first con filing 2013 — real con era; same PDF treatment.

## D. Delisted / index-absent — special fetch — 14 cells
SHRIRAMCIT std ×8: merged into Shriram Finance — BSE detres (§42) + §44 debt-segment.
WESTLIFE std ×10: index has only 24 rows (short history under current symbol) — holdco std can be
tiny but REAL; route = detres → own PDFs; do NOT assume ~0, read it.
KENNAMET ×1 (2003 con): 0 index rows (delisted 2023) — BSE detres/§44.
SPICEJET ×5: index returned 0 rows (retry on fresh session per §55a; then BSE; apply_spicejet_std.py
precedent exists).
(WESTLIFE counted here with its 10; totals overlap B's count by 0.)

## E. XBRL exists but refuses — adjudication-grade, do NOT coerce — 12 cells
EMBDL std ×5 + BEML 2103: XBRL parses but asserts rev=0 (`zero-rev`) — §53b blank-template trap vs
genuine ~0 holdco revenue; confirm against the printed PDF before storing 0.
RAJESHEXPO ×4: `parse-none` (2006) + XBRLs that yield nothing 2103-2109 — format oddity, read PDFs.
ICIL ×2 (2109,2112): parsed value fails neighbour band at 0.01x — a scale error in the FILING
itself or wrong row; manual read.
M&M 2109: filing PAT 1928.64/2491.38 vs stored con PAT −479.08 — one of them is wrong; adjudicate
the stored PAT (§2b) before touching revenue.

## F. Insurers (§55 OCR + §43 IRDAI) — 21 cells
GICRE ×12: 2203–2212 (×4) have GENUINE con-PAT anchors (post-heal) → §55 OCR reads now.
  2003–2112 (×8) stored con PAT == std = COPIES (§55c class) → §2b con-PAT heal FIRST (same
  procedure as the 2026-08-10 MGL/PATANJALI adjudication), then revenue from the same con pages.
NIACL ×4 (2003,2006,2012,2203): §55b solved class; earlier "no filing" results were rate-limit
  false-empties (§55a) — retry with anns_with_retry.
HDFCLIFE ×3 (2003,2006,2009): COVID-era packs where the A5 standalone control failed — re-read;
  if A5 still fails, §43/vision rung (ask first).
LICI 2209 con + 2212 std: early post-IPO filings, §55 route, ratio-family test mandatory.

## G. Proven never-filed — ledger mechanics, not fetching — 3 cells
IOB con 2106,2109,2112: §51c CLOSED (glyph-controlled scans prove std-only until Mar-2022).
The gap definition cannot yet express "started filing con at Mar-22" — teach files_con a
first-con-quarter concept or ledger these three cells; update build_fill_coverage.py,
audit_coverage.py and build_targets.py TOGETHER (the three-copies rule).

## Also in scope when executing: ABB con 2006,2009 (placeholder class, PDF route).
Worktree `~/stocks-wt/revcon-close` holds the staged targets + harvests. Fill-only, provenance
per cell, push through the retry recipe, verify live (§56).
